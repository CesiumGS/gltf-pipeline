'use strict';
var updateVersion = require('../../lib/updateVersion');

describe('updateVersion', function() {
    // Move byteOffset to bufferView works well for the first test model
    it('updateVersion', function() {
    });
});
